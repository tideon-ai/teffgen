# Prometheus config for effGen monitoring

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "effgen-api"
    static_configs:
      - targets: ["effgen-api:8000"]
    metrics_path: /metrics
